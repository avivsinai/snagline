## What this changes and why

## Checklist

- [ ] `make verify` passes locally
- [ ] Tests cover this change, including at least one negative case
      (a rejection or failure-closed path, not only the happy path)
- [ ] Docs updated if this changes behavior, commands, or configuration
      (`README.md`, `docs/`, or `deploy/config/`)
